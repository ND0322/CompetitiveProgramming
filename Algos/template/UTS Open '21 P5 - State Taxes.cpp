#include <bits/stdc++.h>
#include <iostream>

using namespace std;

const int MAXN = 5e5+5;

int n, q, a[MAXN], lazy[MAXN<<2];

long long ans, st[MAXN<<2];


void pushdown(int node, int l, int r){
    if()
}

void build(int node, int l, int r){
    if(l == r) st[node] = a[l];


}