#include <bits/stdc++.h>
#include <iostream>

using namespace std;

int main(){
    //maxamize distance between 1 and 2

    int tt; cin >> tt;

    while(tt--){
        int n; cin >> n;

        int oneIndex,twoIndex = -1;

        for(int i = 1; i <= n; i++){
            int x; cin >> x;
            if(x == 1) oneIndex = i;
            if(x == 2) twoIndex = i;
        }

        x
        

    }
}