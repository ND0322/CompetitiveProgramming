#include <bits/stdc++.h>
#include <iostream>

using namespace std;

void solve(){
    int n,m; cin >> n >> m;
}

int main(){
    //approach: type 3 choices have the least freedom, so we should always serve them first.

    int t; cin >> t;
    while(t--){
        solve();
    }
}