#include <bits/stdc++.h>
#include <iostream>

using namespace std;

int main(){
    int n; cin >> n;

    /*
    few cases

    lets say root has some color a
    parent must have some different color b if we have a parent
    This can only happen if we have <= 1 children

    No parent
    We can paint root any color
    <= 2
    DP  
    */
}