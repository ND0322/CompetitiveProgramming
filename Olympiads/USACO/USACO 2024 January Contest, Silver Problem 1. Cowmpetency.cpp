#include <bits/stdc++.h>
#include <iostream>

using namespace std;


/*
for some q with a[i] and h[i]

all [a[i]+1, h[i]) is <= max(pref[a[i]])
therefore all [1, h[i]) <= max(pref[a[i]])
sort by increasing a[i]


*/