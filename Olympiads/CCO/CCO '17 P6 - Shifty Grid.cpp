#include <bits/stdc++.h>
#include <iostream>

//solve one element at a time
//works for every row except the last

//if the element is on the same row as where it should be
//rotate the element to the spot
//spin the column up
//rotate it back
//sping the column down

//similar for column